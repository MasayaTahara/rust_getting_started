fn main() {
    // Q. 以下のベクターの中の値を昇順にソートしてから、
    // 11で割り切れる値が含まれていたら「割り切れた！」と出力しなさい
    let mut v = vec![45, 16494, 738, 87, 675, 231];
    // 制約1: sortメソッドを使うこと
    // 制約2: イテレータとmapを同じ式で使うこと
    println!("v is {:?}", v);
}
